version: '1.0'
steps:
   deploy:
    image: "otomato/kubectl:alpine"
    working-directory: ${{initial-clone}}
    commands:
      - ls -la
      - export KUBECONFIG=./kubeconfig/config
      - echo ${KUBECONFIG}
      - echo now what
